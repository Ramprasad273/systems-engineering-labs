# Core deep learning framework
torch>=2.4.0

# Tokenization — HuggingFace tokenizers (BPE training and inference)
tokenizers>=0.19.0

# HuggingFace ecosystem utilities (tokenizer config, dataset streaming)
transformers>=4.43.0
datasets>=2.20.0
tqdm>=4.66.0

# NVIDIA Management Library bindings (VRAM peak memory queries)
nvidia-ml-py3>=7.352.0

# YAML configuration parsing
pyyaml>=6.0.1

# Test runner
pytest>=8.0.0
